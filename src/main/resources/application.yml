spring:
  application:
    name: notification-service
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      data-source-properties:
        permitMysqlScheme: true
    url: ${NOTIFICATION_DB_URL:jdbc:mariadb://onehost-amdcloudhn022602.000nethost.com:3306/tsezwyvdhosting_nihongo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${NOTIFICATION_DB_USERNAME:tsezwyvdhosting_nihongo_app}
    password: ${NOTIFICATION_DB_PASSWORD:thuThuy@1}
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    hibernate:
      ddl-auto: update
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: nguyenthang28419902@gmail.com
    password: yclteeqfsqcytyma
    port: 587
    protocol: smtp
    test-connection: false
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          starttls:
            enable: true


  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:180.93.115.154:9092}
    consumer:
      value-deserializer: org.apache.kafka.common.serialization.ByteArrayDeserializer
      auto-offset-reset: earliest
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8083

app:
  frontend:
    url: http://localhost:5173
wallet:
  mail:
    enabled: ${WALLET_MAIL_ENABLED:true}
  events:
    topic: ${WALLET_EVENTS_TOPIC:wallet.events.v1}
    partitions: ${WALLET_EVENTS_PARTITIONS:3}
    replicas: ${WALLET_EVENTS_REPLICAS:1}

